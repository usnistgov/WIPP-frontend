import { ImageAnnotationsCollection } from './image-annotations-collection';

describe('ImageAnnotationsCollection', () => {
  it('should create an instance', () => {
    expect(new ImageAnnotationsCollection()).toBeTruthy();
  });
});
